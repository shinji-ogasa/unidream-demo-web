# Used only by `npm run backfill` (scripts/backfill-history.ts).
# Do NOT commit a filled-in copy — `.env.backfill` is gitignored.

PROJECT_URL=https://YOUR-PROJECT.supabase.co
PROJECT_SECRET_KEY=sb_secret_xxxxxxxxxxxxxxxxx
HF_SPACE_URL=https://shinjiaa-unidream-space.hf.space
HF_INFERENCE_API_KEY=replace-with-the-INFERENCE_API_KEY-set-on-the-Space
